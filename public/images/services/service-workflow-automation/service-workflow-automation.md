# Workflow Automation

**Save the generated image in this exact folder as:** `service-workflow-automation.jpg`

(full path: `public/images/services/service-workflow-automation/service-workflow-automation.jpg`)

Engine validation: **PASS** — built via the `nano-banana-prompt-generator` skill's engine (`engine.schema.build_skeleton()`), checked against the real pulled REF-A through REF-H bible sections, passed `engine.validate.gate(mode="nano")`.

Copy the `full_prompt` field below into ChatGPT and generate at High quality.

```json
{
  "prompt": {
    "scene": "Dark minimal studio with a faint red haze in the air.",
    "subject": "An abstract network of glowing red connected nodes and flowing lines forming a simple flowchart-like structure.",
    "composition": "Diagonal flow from bottom-left to top-right filling the frame, 3D data-visualization render, nodes at varying depths near the frame edge, shallow depth of field with soft bokeh on distant nodes.",
    "lighting": "Self-illuminated red glow at 3000K from the nodes and connector lines, ambient dark background, subtle 5500K white rim highlight on the nearest node, Kelvin-locked.",
    "materials": "Glass-like glowing nodes with a slightly uneven surface facet, thin brushed-metal connector lines with a small tarnish mark.",
    "style": "Abstract 3D data-visualization render, sleek and technical, no readable text anywhere."
  },
  "technical_params": {
    "aspect_ratio": "4:5",
    "resolution": "2K",
    "image_config": {
      "aspect_ratio": "4:5",
      "image_size": "2K"
    },
    "output_format": "PNG — DEFAULT. JPEG only if file size is a hard constraint."
  },
  "category": "product_shot",
  "full_prompt": "Create abstract 3D data-visualization render, sleek and technical, no readable text anywhere. Scene: Dark minimal studio with a faint red haze in the air. Subject: An abstract network of glowing red connected nodes and flowing lines forming a simple flowchart-like structure. Composition: Diagonal flow from bottom-left to top-right filling the frame, 3D data-visualization render, nodes at varying depths near the frame edge, shallow depth of field with soft bokeh on distant nodes. Lighting: Self-illuminated red glow at 3000K from the nodes and connector lines, ambient dark background, subtle 5500K white rim highlight on the nearest node, Kelvin-locked. Materials: Glass-like glowing nodes with a slightly uneven surface facet, thin brushed-metal connector lines with a small tarnish mark. Throughout, preserve natural micro-imperfections visible under close inspection, soft shadow falloff across the full tonal range, and a commercial rendering and photography technical standard. Portrait orientation, 4:5 aspect ratio."
}
```
