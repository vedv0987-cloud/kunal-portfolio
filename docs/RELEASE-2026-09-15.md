# Portfolio motion and navigation release — 15 September 2026

## Shipped in this batch

- Navigation: Home → Services → Work → Tools → Pricing → About. Contact appears only in the footer; all site Contact/start-project calls to action lead to Pricing. Direct email and phone remain in the footer.
- Hero: rotating Design/Create/Imagine, Automate/Accelerate/Streamline, Scale/Elevate/Lead copy with stable layout. Removed the Claude-logo montage frame and reused the clean workflow and light-bulb artwork.
- Pricing: rotating outcome headline, animated prices, working proposal dialog.
- Shared motion: word entrances, scroll-driven reveals, repeat-on-entry counters, floating highlight text, cursor tilt/spotlights and smooth desktop wheel scrolling. Native touch scrolling and reduced-motion fallbacks remain available.
- Tools: editorial heading, logo ribbons, counters, sticky category filters, consistent cards and arrow-key/Home/End tab navigation.
- QA tools: browser smoke supports screenshots in this local checkout, and checks brand assets from the actual repository. Startup works in both local clones and the original sandbox.

## Validation

`npm run typecheck` and `npm run build` pass. Desktop and 390px mobile smoke checks pass on Home, Tools and Pricing. Built Home matches the development baseline. Browser interaction QA checks CTA routing, headline rotation, smooth wheel scrolling, proposal dialog opening/closing, Tools filters and keyboard controls, six additional routes at both sizes, loaded images, mobile navigation and reduced motion. No uncaught browser errors or horizontal overflow found.

The agent-browser CLI failed twice to resolve a visible CTA, so interactive verification used `scripts/portfolio-qa.mjs` with Playwright. The sandbox-only `preview:restart` cannot run on macOS; built-output verification used the existing `npm run preview` command instead.

## Image audit and generation priority

Home, Services, About and Contact already use artwork. Two of nine Work records have covers; the seven below still use intentional graphic fallbacks. Their detail-page covers also depend on the same image records.

Use the complete prompts in [image-prompts-missing-2026-09-15.md](image-prompts-missing-2026-09-15.md). Generate one image per prompt, with no baked text, invented client logo or fabricated campaign metrics. These are concept illustrations; use approved real deliverables for documentary case studies.

| Priority | Surface | Prompt in existing prompt file | Output |
|---|---|---|---|
| Required artwork | Reliance Foundation Hospital | Work 1 | 16:9 hospital editorial scene |
| Required artwork | Blu Diamond | Work 2 | 16:9 luxury architecture |
| Required artwork | Claude AI Automation | Work 3 | 16:9 abstract workflow interface |
| Required artwork | OncoSphere | Work 4 | 16:9 medical education |
| Required artwork | Realatte AI | Work 5 | 16:9 property technology |
| Required artwork | AlphaMed | Work 6 | 16:9 healthcare product still life |
| Required artwork | ESMO Asia | Work 7 | 16:9 unbranded congress booth |
| Optional | Pricing Starter/Pro/Premium/Enterprise | Pricing 1–4 | Four coordinated 4:3 illustrations |
| Optional | Tools hero | Tools hero | 21:9 creative workstation |
| Optional future content | Cinematic video | Work 8 | Not a missing current Work record |

The Tools page already has real brand icons and does not require a banner to function. Pricing currently uses icons and clear pack comparisons. Testimonials do not require generated portraits; use real, approved client photos if desired. Do not render full-page reference boards as website images.

## Next integration plan

Vercel project: `kunal-portfolio`. GitHub: `vedv0987-cloud/kunal-portfolio`. Both CLIs are authenticated. Vercel environment-variable inventory returned no configured variables on 15 September 2026.

1. **Enable existing proposal email delivery.** Configure `RESEND_API_KEY`, `PROPOSAL_FROM_EMAIL` (verified sender), and optional `OWNER_EMAIL` in Vercel. The implementation already exists in `src/lib/proposal.server.ts`. Redeploy, then make one explicitly authorized test submission to a real destination and confirm receipt. Never mark delivery working just because the dialog opens.
2. **Connect the legacy contact brief form.** It still saves a local browser draft. Reuse the existing server email approach; show success only after the provider accepts delivery, preserve drafts on failure, validate input server-side and keep recipients server-controlled.
3. **Add booking.** Use the owner's real Cal.com event URL. Keep booking user-initiated; do not publish the roadmap's example username.
4. **Protect production submissions.** Add server-verified Turnstile and a durable rate limiter before scaling traffic. The existing in-memory limiter is best-effort on serverless infrastructure.
5. **Add conversion measurement.** Choose one analytics provider, then track start-project, pack selection, successful brief delivery and booking clicks.
6. **Replace remaining case-study artwork and verify facts.** Integrate the seven covers above, then confirm project claims, client permissions and testimonials with the owner.

Accounts and a database are not needed for this visual release. No credentials were added to source control. The previously excluded `public/images/reference-assets-2026-09-14/` reference pack stays local and is not part of this commit.
